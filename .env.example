# -----------------------------------------------------
# FAM Trainingsplan - Environment Variables Template
# -----------------------------------------------------
# Copy this file to .env for local development
#
# IMPORTANT: Never commit .env to version control!
# Add .env to .gitignore
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   # Use direnv for auto-loading or source manually

# Development mode
NODE_ENV=development

# Vite dev server configuration
VITE_PORT=5173
VITE_HOST=localhost

# Playwright configuration
PLAYWRIGHT_BROWSERS_PATH=~/.cache/ms-playwright

# Use system browsers instead of downloading Playwright browsers
# Set to 'true' on Arch Linux to use system chromium/firefox
# USE_SYSTEM_BROWSERS=true

# Test configuration
HEADLESS=false  # Set to true for CI/headless testing

# Project metadata
PROJECT_NAME=fam-trainingsplan
PROJECT_VERSION=2.4.0

# -----------------------------------------------------
# Optional: API Keys (if needed in future)
# -----------------------------------------------------
# GOOGLE_MAPS_API_KEY=your_api_key_here
# ANALYTICS_ID=your_analytics_id

# -----------------------------------------------------
# Optional: Debug flags
# -----------------------------------------------------
# DEBUG=vite:*
# BROWSER=chromium
